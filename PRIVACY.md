# 隐私说明

Mac助手 0.2.0 是本地优先的个人工具，不接入第三方遥测、广告 SDK 或云端剪贴板服务。

## 剪贴板

- 完成首次隐私说明前不会读取或记录剪贴板。
- 历史库使用 AES-256-GCM 加密；去重值使用 HMAC-SHA256，数据库、文本、富文本、图片、blob 和缩略图中不保存可搜索明文。
- 256 位密钥保存在 macOS 钥匙串，访问级别为 `AfterFirstUnlockThisDeviceOnly`，不会随配置导出或 iCloud 备份同步。
- 默认记录开启、保留 30 天；收藏记录不会按保留期删除。密码管理器、自定义排除 App，以及带 concealed、transient、auto-generated 标记的内容会跳过。
- 缩略图只在历史面板显示期间解密到权限为 `0600` 的临时缓存，关闭面板或退出应用即清理。
- 密钥缺失或损坏时记录会暂停；应用不会自动删除历史或重建密钥。

## 配置、日志与诊断

- 配置、运行状态、数据库、日志和 Finder 桥接凭据只写入当前用户目录，目录权限为 `0700`、文件权限为 `0600`。
- 日志为本地结构化事件，用户目录会脱敏，按 2 MB × 3 轮转。
- 异常退出标记和诊断导出保存在本地，不会自动上传。
- iCloud 只备份带版本与时间戳的应用配置，不包含剪贴板密钥、剪贴板内容或 Finder 桥接密钥。

数据位置和清理方法见 [docs/recovery.md](docs/recovery.md)。
