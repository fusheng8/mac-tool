# 更新记录

## 0.2.5

- 修复内置屏和外接屏均保存为“希望关闭”时，内置显示器被错误判定为会关闭全部屏幕的问题。
- 手动关闭改为依据当前实际亮屏状态判断；只要仍有另一块亮屏，就允许关闭选中的内置显示器。
- 修复安全守护在外接屏仍亮着时错误重新打开内置屏的问题，同时继续保护最后一块可用显示器。

## 0.2.4

- 将主窗口、菜单栏菜单、卸载、压缩、端口、剪贴板和首次引导界面统一还原为 0.2.1 的布局与交互。
- 保留 0.2.2–0.2.3 的配置、端口安全、Finder 目标应用、剪贴板存储和恢复线程安全等内部优化。
- 修复显示器关闭后 30 秒自动恢复的问题；升级后默认持续关闭，直到用户手动重新打开。
- 保留“只允许关闭外接显示器”开关，允许用户明确关闭限制后操作 MacBook 内置显示屏。
- 清理旧的显示器恢复队列，永久关闭不再被误报为“等待恢复”，同时修复状态数量显示为变量名的问题。

## 0.2.3

- 将主窗口重构为分组导航的控制中心，并用自绘菜单栏浮窗统一常用工具入口和运行状态。
- 重构剪贴板、显示器、Finder 增强、压缩工具、端口管理与应用卸载页面，降低默认信息密度并强化安全确认。
- 新增 Finder 菜单显示名称与目标应用配置、压缩预设、端口进程聚合、应用卸载检查面板和四步首次引导。
- 移除“最近活动/最近任务”，将权限、诊断、备份、更新和危险操作集中到偏好设置。
- 恢复显示器“只允许关闭外接显示器”开关，并正确保存对内置显示屏的操作偏好。
- 完善控制中心深链路、窗口状态恢复、浅色/深色界面和配置 schema v3 迁移。

## 0.2.2

- 修复显示器严格/加权匹配、同名设备误识别与匹配歧义，设置页开放匹配模式和阈值。
- 修复 UDP 客户端连接误报为监听端口，并在停止进程前校验 PID 启动时间、路径和当前端口占用。
- 改进剪贴板复制来源归因，支持多 pasteboard item 加密保存与按原分组粘贴。
- 统一配置数值规范化和 Finder 配置副本同步，避免异常导入崩溃及磁盘/内存状态分裂。
- 增加可选的本地 Developer ID Application 签名与 Apple 公证脚本接口。

## 0.2.1

- 内置 7-Zip 26.02 arm64 引擎，统一 ZIP、TAR、GZIP、BZIP2、XZ、7Z 和 RAR 的读取、创建与解压流程。
- 增强 ZIP64、AES 加密 ZIP、历史编码 ZIP、RAR 4/5 以及压缩包条目管理的兼容性。
- 增加压缩任务进度、取消、密码验证、路径穿越防护与冲突文件保留策略。
- 完善 Finder 右键菜单与压缩设置交互，并补充归档引擎的回归测试和发布校验。
- 在 README 中记录 m1ddc、Mole CLI、GRDB.swift、Sparkle、7-Zip 和 libarchive 等开源项目的参考与使用情况。

## 0.2.0

- 新增首次隐私引导和升级用户的隐私与安全确认；完成前不启动后台剪贴板或显示器任务。
- 剪贴板历史迁移为 Keychain + AES-256-GCM 加密库，加入敏感标记过滤、后台防抖搜索和临时缩略图缓存。
- Finder 桥接请求加入 HMAC-SHA256 签名、30 秒时效、UUID 防重放、动作与路径校验；删除型解压仍需二次确认。
- 移除个人显示器默认配置，显示器软断开默认关闭，并加入能力隔离、自触发抑制、指数退避、熔断和精准恢复。
- 配置增加 schema 版本、串行事务写入、损坏副本恢复和安全权限；iCloud 使用版本化备份封装。
- 新增 Swift 6 `MacToolCore` 模块，集中承载加密原语、安全策略、端口解析与归档规则。
- 日志改为本地结构化事件、路径脱敏和 2 MB × 3 轮转；通知迁移到 UserNotifications。
- 版本统一为 `VERSION` 文件，发布目标固定为 arm64/macOS 13+；构建与安装拆分，正式构建强制固定签名。
